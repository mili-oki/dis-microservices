spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/authdb
    username: auth
    password: auth
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 0
