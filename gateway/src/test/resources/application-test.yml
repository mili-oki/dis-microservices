spring:
  application:
    name: gateway
  config:
    import: ""         # bez uvoza sa config-servera
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false   # bez Eureke u testu

eureka:
  client:
    enabled: false

management:
  endpoints.web.exposure.include: health,info

# dummy JWT samo da prođe konstrukcija beansa
auth:
  jwt:
    issuer: test
    secret: test-test-test-test-test-test-test-test-1234567890
